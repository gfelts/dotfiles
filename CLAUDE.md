# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- **OS**: Debian-based Linux on Proxmox VE (kernel 6.8.12-17-pve)
- **Shell**: bash
- **User tools**: Claude Code (`~/.local/bin/claude`), tmux, curl, apt
- **PATH**: `~/.local/bin` is prepended to PATH (set in `~/.bashrc`)

## Notes

This is a general-purpose home directory, not a specific project repository. When the user begins working on a project here, update this file with project-specific build commands, architecture notes, and conventions.
